// Sonner toast notifications have been replaced by inline feedback.
// This component is kept as a no-op to avoid breaking the component tree.
const MessageDisplay = () => null;

export default MessageDisplay;
